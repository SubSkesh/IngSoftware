spring.application.name=simple-inventory-system
spring.mvc.view.prefix=/WEB-INF/templates/
spring.mvc.view.suffix=.jsp


server.port=8081


# Configurazione del database MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/inventorydb
spring.datasource.username=root
spring.datasource.password=oscarinonabbo00
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialetto SQL specifico per MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect

# Creazione e aggiornamento delle tabelle
spring.jpa.hibernate.ddl-auto=update

# Opzioni di log SQL (opzionale, per debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
